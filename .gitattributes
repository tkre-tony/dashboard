# PropertyAtlas line-ending policy
#
# Root index.html is LF-only (measured: 0 CR bytes). The newsroom monolith
# at newsroom/index.html is the CRLF file — DO NOT normalize that one.
# (No rule below targets either, so Git leaves both as committed.)
# Generated news pages and tooling scripts are LF. Pin them so a
# Windows working-tree (download/extract/OneDrive) cannot flip them
# to CRLF on commit.

news/**/*.html  text eol=lf
news/*.html     text eol=lf
tools/*.js      text eol=lf
tools/*.py      text eol=lf
